import 'package:flutter/material.dart';

class AppColors {
  // Dark theme
  static const backgroundDark = Color(0xFF020617); // deep navy/black
  static const surfaceDark = Color(0xFF030712);    // slightly lighter
  static const surfaceDarkElevated = Color(0xFF060A1A);

  static const primary = Color(0xFF8B5CF6);        // violet
  static const secondary = Color(0xFF22D3EE);      // cyan accent

  static const textPrimaryDark = Color(0xFFF9FAFB);
  static const textSecondaryDark = Color(0xFF9CA3AF);

  // Light theme
  static const backgroundLight = Color(0xFFF9FAFB);
  static const surfaceLight = Colors.white;

  static const textPrimaryLight = Color(0xFF020617);
  static const textSecondaryLight = Color(0xFF4B5563);

  static const error = Color(0xFFF97373);
}
